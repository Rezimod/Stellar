/**
 * TYCHO, the first card: a crater rather than a planet, on purpose. The
 * telescope is pointed at the Moon, and the card is one place on it. Filed in
 * First Light; kept here by name for the lifecycle script and the tests.
 */

import { SET_001_CARD_BY_DESIGNATION } from '@/lib/sets/set-001';

export const TYCHO_CARD = SET_001_CARD_BY_DESIGNATION.get('TYCHO')!;

export const TYCHO = TYCHO_CARD.seed;
